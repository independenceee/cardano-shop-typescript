import React from "react";

type Props = {};

const CartProducts = function () {
    if (true) {
        return (
            <div className="flex flex-col align-center w-full h-full">Pro</div>
        );
    }
};

export default CartProducts;

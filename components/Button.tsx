import React from "react";

type Props = {
    width: number | string;
    height: number | string;
    handle: () => void;
    title: string;
    icon: string | null;
};

const Button = function ({ width, height, handle, title, icon }: Props) {
    return <></>;
};

export default Button;
